export function upgradePickaxe(dispatch: React.Dispatch<any>) {
    dispatch({ type: "UPGRADE_PICKAXE" });
}

export function upgradeUserLevel(dispatch: React.Dispatch<any>) {
    dispatch({ type: "UPGRADE_USER_LEVEL" });
}